function pointsClear(id){
    var element = document.querySelector(`#${id}`)
    element.innerText++;
}
function pointsUp(id){
    var element = document.querySelector(`#${id}`)
    element.innerText++;
}
